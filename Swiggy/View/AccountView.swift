import SwiftUI

struct AccountView: View {
    
    
    var body: some View {
        VStack {
            // Text
            Text("Breakfast")
                .font(.title)
                .fontWeight(.black)
                .foregroundColor(Color.gray)
                .multilineTextAlignment(.leading)
            // Async Image
            
        }
    }
}

#Preview {
    AccountView()
}
